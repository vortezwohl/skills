interface:
  display_name: "Prompt Engineering"
  short_description: "Design and validate structured prompts"
  default_prompt: "Use $prompt-engineering to design, rewrite, or review a structured prompt with a strict output contract."

policy:
  allow_implicit_invocation: true
